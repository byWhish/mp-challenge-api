const users = require('./users.json')

const get = (id) => {
    return null
}

const add = (user) => {
    return null
}

const all = () => {
    return users
}

module.exports = {
    get,
    all,
    add,
}
